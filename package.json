{
  "name": "himalayahub-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "backend",
    "blockchain"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "pnpm --filter @himalayahub/database db:generate"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
